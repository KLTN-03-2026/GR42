// Nếu đổi tên thư mục, chỉ cần sửa giá trị này ở đây.
export const PROJECT_FOLDER = '/GR42';

export const API_BASE_URL = `http://localhost${PROJECT_FOLDER}/BE`;

// URL của trang React (thường là http://localhost:3000 hoặc 3002)
export const FRONTEND_URL = window.location.origin; 
